/.vscode/
**/.west/
**/build/

# Secrets — never commit secrets.env (the SD-card provisioning
# loader; values are user-supplied). secrets.env.example is the
# placeholder version and IS committed.
/secrets.env
*.env.local
