# SSH server policy for the kitchen-clock main unit.
# Drop-in: included by /etc/ssh/sshd_config.d/ at boot, after the
# stock config has been read.
PasswordAuthentication yes
PubkeyAuthentication yes
ChallengeResponseAuthentication no
PermitRootLogin no
ClientAliveInterval 60
ClientAliveCountMax 6
